SQLite database added to the tasks CRUD API presented in the previous repo.

HOW TO USE :
run the Main.js file using "node Main.js" in your editor terminal then use the requests represented in "Testing.http" to test the server.

Used DB Browser for SQLite to update and delete data then recreated one using an http request to make understand how database file can
work across many apps with Realtime data updates.
